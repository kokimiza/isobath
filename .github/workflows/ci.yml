# CI for every push / PR, and API deploy to Render after main passes.
# Render auto-deploy must be OFF: this workflow is the only thing that deploys the API,
# so a commit that fails lint or tests never reaches production.
name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  api:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: app
    services:
      # plain PostgreSQL + tests/db/supabase_stub.sql: RLS / constraint / nightly batch tests
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          version: '0.12.17'
      - run: uv sync --frozen
      - run: uv run --no-sync ruff check
      - run: uv run --no-sync ruff format --check
      - run: uv run --no-sync pytest -q
        env:
          ISOBATH_TEST_PG: postgresql://postgres:postgres@127.0.0.1:5432/postgres

  web:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      # Build-time public values stored in GitHub repository Actions secrets.
      PUBLIC_SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}
      PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
      PUBLIC_API_BASE: ${{ secrets.PUBLIC_API_BASE }}
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v4 # version from package.json "packageManager"
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build # also compiles paraglide messages needed by check
      - run: pnpm check
      - run: pnpm exec prettier --check .
      - run: pnpm exec eslint .

  deploy-api:
    needs: [api, web]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Did the API change?
        id: changes
        run: |
          before='${{ github.event.before }}'
          if [ -z "$before" ] || [ "$before" = 0000000000000000000000000000000000000000 ] \
             || ! git cat-file -e "$before" 2>/dev/null \
             || [ -n "$(git diff --name-only "$before" "$GITHUB_SHA" -- app)" ]; then
            echo "deploy=true" >> "$GITHUB_OUTPUT"
          else
            echo "deploy=false" >> "$GITHUB_OUTPUT"
            echo "No changes under app/: skipping Render deploy"
          fi
      - name: Deploy this commit to Render and wait until live
        if: steps.changes.outputs.deploy == 'true'
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ vars.RENDER_SERVICE_ID }}
        run: |
          set -euo pipefail
          : "${RENDER_API_KEY:?secret RENDER_API_KEY is not set}"
          : "${RENDER_SERVICE_ID:?variable RENDER_SERVICE_ID is not set}"
          api="https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys"

          # Render API call that prints Render's error body instead of hiding it (curl -f would).
          render() {
            local body code
            body=$(mktemp)
            code=$(curl -sS -o "$body" -w '%{http_code}' \
              -H "Authorization: Bearer $RENDER_API_KEY" -H "Accept: application/json" "$@")
            if [ "$code" -lt 200 ] || [ "$code" -ge 300 ]; then
              echo "::error::Render API returned HTTP $code: $(cat "$body")" >&2
              case "$code" in
                401) echo "::error::Check the RENDER_API_KEY secret" >&2 ;;
                404) echo "::error::Service $RENDER_SERVICE_ID not visible to this API key, or commit $GITHUB_SHA not in the service's repository/branch" >&2 ;;
              esac
              return 1
            fi
            cat "$body"
          }

          id=$(render -H "Content-Type: application/json" \
                 -d "{\"commitId\":\"$GITHUB_SHA\",\"clearCache\":\"do_not_clear\"}" "$api" | jq -r .id)
          echo "deploy $id for $GITHUB_SHA"
          echo "https://dashboard.render.com/web/$RENDER_SERVICE_ID/deploys/$id"
          for _ in $(seq 1 90); do
            status=$(render "$api/$id" | jq -r .status)
            echo "$(date -u +%T) $status"
            case "$status" in
              live) exit 0 ;;
              build_failed|update_failed|pre_deploy_failed|canceled|deactivated) exit 1 ;;
            esac
            sleep 10
          done
          echo "timed out waiting for Render"; exit 1
